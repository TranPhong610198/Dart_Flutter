spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myfschool_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    clean-disabled: false # Tạm để false để lúc test bạn có thể dọn DB nếu cần
    clean-on-validation-error: true